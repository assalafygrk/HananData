name: hanan_data
description: HananData — Nigerian VTU Service App
publish_to: 'none'
version: 2.4.1+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^22.2.0
  google_fonts: ^8.2.0
  http: ^1.6.0
  local_auth: ^3.0.2
  share_plus: ^13.3.0
  shared_preferences: ^2.5.5
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/images/providers/
